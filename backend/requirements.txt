# --- HMS backend (FastAPI + Supabase) ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
python-jose[cryptography]>=3.3      # verify Supabase JWTs
asyncpg>=0.29                        # direct Postgres access (business logic)
httpx>=0.27
structlog>=24.1                      # structured logging
python-dotenv>=1.0

# --- dev / test ---
pytest>=8.0
pytest-asyncio>=0.23
